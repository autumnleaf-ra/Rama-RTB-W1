function leftSlide(arr) {
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
  }

  //   for (let i = 0; i < myNums.length; i++ ) {
  //     sum += myNums[i];
  //   }
}

leftSlide([2, 2, 2, 0]); // expected [4, 2, 0, 0] Merge left-most tiles first.
